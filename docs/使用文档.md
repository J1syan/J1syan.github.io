# 博客使用文档

## 项目信息

- 本地目录：`/home/j1syan/Documents/J1syan.github.io`
- 推荐 GitHub 仓库：`J1syan/J1syan.github.io`
- 线上地址：`https://j1syan.github.io/`
- 技术栈：Astro、Markdown/MDX、GitHub Pages
- 已启用功能：文章、分类、标签、RSS、sitemap、GitHub Actions 自动部署

## 目录结构

```text
.
├── .github/workflows/deploy.yml   # GitHub Pages 自动部署
├── docs/使用文档.md               # 当前文档
├── public/                        # 静态资源
├── src/
│   ├── components/                # 页面组件
│   ├── content/blog/              # 博客文章
│   ├── layouts/                   # 文章布局
│   ├── lib/blog.ts                # 文章、分类、标签工具函数
│   ├── pages/                     # 路由页面
│   └── styles/global.css          # 全局样式
├── astro.config.mjs               # Astro 配置
├── package.json
└── README.md
```

## 本地开发

```bash
cd /home/j1syan/Documents/J1syan.github.io
npm install
npm run dev
```

浏览器打开：

```text
http://localhost:4321/
```

停止开发服务：在终端按 `Ctrl+C`。

## 新增文章

在 `src/content/blog/` 目录下创建一个 `.md` 文件，例如 `my-note.md`：

```md
---
title: '我的学习笔记'
description: '这是一篇学习笔记摘要'
pubDate: '2026-04-30'
category: '学习笔记'
tags: ['Java', 'Spring Cloud']
draft: false
---

正文写在这里。
```

发布规则：

- 文件名会影响文章 URL，例如 `my-note.md` 对应 `/blog/my-note/`
- `draft: true` 的文章不会出现在页面和 RSS 中
- `category` 会自动生成分类页
- `tags` 会自动生成标签页

## 修改站点信息

站点标题、简介、作者和 URL 在 `src/consts.ts`：

```ts
export const SITE_TITLE = '我的博客';
export const SITE_DESCRIPTION = '学习与生活';
export const SITE_AUTHOR = 'J1syan';
export const SITE_URL = 'https://j1syan.github.io';
```

## 修改导航

导航在 `src/components/Header.astro`。

当前导航包含：

- 首页：`/`
- 文章：`/blog/`
- 分类：`/categories/`
- 标签：`/tags/`
- 关于：`/about/`

## GitHub Pages 部署

首次创建 GitHub 仓库时，建议仓库名使用：

```text
J1syan.github.io
```

本地关联并推送：

```bash
cd /home/j1syan/Documents/J1syan.github.io
git branch -M main
git remote add origin git@github.com:J1syan/J1syan.github.io.git
git add .
git commit -m "init blog"
git push -u origin main
```

然后打开 GitHub 仓库：

```text
Settings -> Pages -> Build and deployment -> Source -> GitHub Actions
```

项目已经包含 `.github/workflows/deploy.yml`，推送到 `main` 后会自动构建并部署。

## 安全提醒

不要把 GitHub token、密码、密钥写入仓库。你之前发出的 token 建议在 GitHub 中撤销并重新生成。

后续推送建议使用 SSH key 或 GitHub CLI 登录，不要把 token 写进代码、文档、远程地址或 `.env` 文件。

## 常见维护

构建检查：

```bash
npm run build
```

预览构建结果：

```bash
npm run preview
```

升级依赖：

```bash
npm update
npm run build
```
